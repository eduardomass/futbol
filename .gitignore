# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variables de entorno
# `.env` SI se versiona: solo contiene la Project URL y la publishable key de
# Supabase, que son publicas por diseno (viajan dentro del bundle JS del navegador).
# Lo que protege la base es RLS. La service_role key NUNCA va en el repo.
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
